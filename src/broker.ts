// Re-exported from @sproutboat/wire (moved verbatim). This shim keeps
// every `./broker` importer and the `sproutboat/runtime/broker` export path
// working; the implementation lives in the shared package.
export * from "@sproutboat/wire";
