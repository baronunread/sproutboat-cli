// Re-exported from @sproutboat/artifact (moved verbatim). This shim keeps
// every `./manifest` importer and the `sproutboat/runtime/manifest` export
// path working; the implementation lives in the shared package.
export * from "@sproutboat/artifact";
