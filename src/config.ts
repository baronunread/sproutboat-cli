// Re-exported from @sproutboat/config (moved verbatim). This shim keeps
// every `./config` importer and the `sproutboat/runtime/config` export path
// working; the implementation lives in the shared package.
export * from "@sproutboat/config";
