{
  "name": "sproutboat-kitchen-sink",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd web && bun run build",
    "deploy": "bun run build && bunx sproutboat deploy",
    "dev": "bun serve.ts",
    "test": "bun harness.ts"
  }
}
